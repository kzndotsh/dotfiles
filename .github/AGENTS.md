# AGENTS.md — .github

> Scope: `.github` — inherits [`AGENTS.md`](../AGENTS.md) unless noted.

GitHub metadata. **No Actions workflows** (the old eval-on-push yaml was removed). Not part of the NixOS build.

Do not re-add CI that needs secrets or that deploys (`nh os switch`, tofu apply).
